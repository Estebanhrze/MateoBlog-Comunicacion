<footer>
  <div class="social-links" aria-label="Redes sociales">
    <a href="https://www.instagram.com/" target="_blank" rel="noopener noreferrer" aria-label="Instagram" title="Instagram">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <rect x="3" y="3" width="18" height="18" rx="5"></rect>
        <circle cx="12" cy="12" r="4"></circle>
        <circle class="icon-dot" cx="17.5" cy="6.5" r="1"></circle>
      </svg>
    </a>
    <a href="https://www.tiktok.com/" target="_blank" rel="noopener noreferrer" aria-label="TikTok" title="TikTok">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <path d="M15 4v10.5a4.5 4.5 0 1 1-4-4.25"></path>
        <path d="M15 4c.5 2.5 2 4 4.5 4.5"></path>
      </svg>
    </a>
    <a href="https://www.linkedin.com/" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn" title="LinkedIn">
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <rect x="3" y="3" width="18" height="18" rx="2"></rect>
        <path d="M8 10v7"></path>
        <path d="M8 7v.01"></path>
        <path d="M12 17v-4a3 3 0 0 1 6 0v4"></path>
        <path d="M12 10v7"></path>
      </svg>
    </a>
  </div>
  <span>
    &copy; {new Date().getFullYear()}
    <a> Mateo</a>.
  </span>
</footer>

<style>
  footer {
    color: var(--text-secondary);
    font-size: .8em;
    margin: 1em auto;
    max-width: 1400px;
    padding: 1em 2em;
    text-align: center;
    width: 100%;
  }

  .social-links {
    align-items: center;
    display: flex;
    gap: 0.65rem;
    justify-content: center;
    margin-bottom: 0.65rem;
  }

  .social-links a {
    align-items: center;
    border: 1px solid var(--text-secondary);
    border-radius: 50%;
    color: var(--text-secondary);
    display: inline-flex;
    height: 38px;
    justify-content: center;
    transition: border-color 180ms ease, color 180ms ease, transform 180ms ease;
    width: 38px;
  }

  .social-links a:hover {
    border-color: var(--primary-color);
    color: var(--primary-color);
    transform: translateY(-2px);
  }

  .social-links a:focus-visible {
    outline: 2px solid var(--primary-color);
    outline-offset: 3px;
  }

  .social-links svg {
    fill: none;
    height: 20px;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.8;
    width: 20px;
  }

  .social-links .icon-dot {
    fill: currentColor;
    stroke: none;
  }

  @media (prefers-reduced-motion: reduce) {
    .social-links a {
      transition: none;
    }
  }
</style>
